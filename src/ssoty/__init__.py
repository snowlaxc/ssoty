"""ssoty — static cross-harness rule coherence auditor.

Deterministic, no LLM, no network. Reads agent-harness configuration
(Claude Code, Codex, ...) and reports where shared rules fail to apply
consistently across harness boundaries.
"""

__version__ = "0.1.8"
