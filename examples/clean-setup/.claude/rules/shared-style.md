# Shared Style (acme-corp)

Keep diffs small. Test before refactoring. Prefer deletion over abstraction.
No dependency without approval. Commits explain why, not what.

Team formation: see `team-rules.md` (claude-only). Layout: `meta-layout.md`.
